const MAX_STEP = 3

const SIZE = 32

const DIRECTION = {
  DOWN: 0,
  LEFT: SIZE * 1,
  RIGHT: SIZE * 2,
  UP: SIZE * 3
}
//rules after submit
const limitSensorsToBoard=10
export { MAX_STEP, SIZE, DIRECTION,limitSensorsToBoard }
